{% extends 'base.html.twig' %}

{% block title %}Authors{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h1 class="text-center mb-4">Authors</h1>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Welcome to the Authors section!</h5>
                    <p class="card-text">This is where you can manage authors and their information.</p>
                    <a href="{{ path('app_showAuthor', {'name': 'Victor Hugo'}) }}" class="btn btn-primary">View Author</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}